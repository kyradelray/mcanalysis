% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{mc_analysis}
\alias{mc_analysis}
\alias{mc_analyze}
\title{Menstrual Cycle Analysis}
\usage{
mc_analysis(
  period_dates,
  outcome_data,
  confounder_data = NULL,
  id_col = "id",
  date_col = "period_date",
  outcome_col = "outcome",
  outcome_date_col = "date",
  min_cycle_length = 21,
  max_cycle_length = 35,
  min_negative_obs = 5,
  min_positive_obs = 5,
  k = 10,
  confounder_cols = NULL
)

mc_analyze(
  period_dates,
  outcome_data,
  confounder_data = NULL,
  id_col = "id",
  date_col = "period_date",
  outcome_col = "outcome",
  outcome_date_col = "date",
  min_cycle_length = 21,
  max_cycle_length = 35,
  min_negative_obs = 5,
  min_positive_obs = 5,
  k = 10,
  confounder_cols = NULL
)
}
\arguments{
\item{period_dates}{Data frame with user ID and period start dates}

\item{outcome_data}{Data frame with user ID, date, and outcome values}

\item{confounder_data}{Optional data frame with confounders}

\item{id_col}{Column name for user ID (default "id")}

\item{date_col}{Column name for period dates (default "period_date")}

\item{outcome_col}{Column name for outcome variable (default "outcome")}

\item{outcome_date_col}{Column name for outcome dates (default "date")}

\item{min_cycle_length}{Minimum cycle length to include (default 21)}

\item{max_cycle_length}{Maximum cycle length to include (default 35)}

\item{min_negative_obs}{Minimum observations with negative cycle days (default 5)}

\item{min_positive_obs}{Minimum observations with positive cycle days (default 5)}

\item{k}{Number of basis functions for GAM (default 10)}

\item{confounder_cols}{Specific confounder columns to analyze (default NULL uses all)}
}
\value{
List with analysis results
}
\description{
Run complete menstrual cycle analysis pipeline.
}
\examples{
\dontrun{
# Load your data
periods <- read.csv("period_dates.csv")
outcomes <- read.csv("outcomes.csv")

# Run analysis
results <- mc_analysis(periods, outcomes)

# View results
print(results$summary)
plot(results)
}
}
